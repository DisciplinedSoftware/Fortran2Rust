{
  "name": "Fortran2Rust",
  "build": { "dockerfile": "Dockerfile" },
  "workspaceFolder": "/workspaces/Fortran2Rust",
  "postCreateCommand": "pip install -e '.[dev]'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "rust-lang.rust-analyzer",
        "ekho.fortran-language-server",
        "george-alisson.html-preview-vscode"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/venv/bin/python3"
      }
    }
  },
  "remoteEnv": {
    "PATH": "/opt/venv/bin:${containerEnv:PATH}:/home/vscode/.cargo/bin"
  }
}
